apm-server:
  host: '${APM_HOST}'
  rum: 
    enabled: true
    allow_origins: '${ALLOWED_ORIGINS}'
  kibana:
    enabled: true
    host: '${KIBANA_HOST}'
output.elasticsearch:
  proxy_url: '${ES_HOSTS}'
  